# SAP AI Flow 用户操作手册

> 文档版本：1.0
>
> 更新日期：2026-08-10
>
> 适用对象：SAP 顾问、流程编辑者、顾问审批者、项目管理员和查看者

本手册说明如何使用 SAP AI Flow 创建、维护、审核、发布和导出 SAP 业务流程。部署、备份、监控、OIDC 配置及故障恢复由运维人员按 [deploy/OPERATIONS.md](deploy/OPERATIONS.md) 执行。

## 1. 开始前须知

### 1.1 三类流程对象

系统将以下对象分别管理，操作时应先确认目标类型：

| 对象 | 用途 | 主要操作入口 |
| --- | --- | --- |
| 节点 | 表示开始、结束、任务、判断或子流程 | 底部自然语言指令、画布节点、右侧“节点属性” |
| 连线 | 表示节点之间的顺序或条件路径 | 拖动节点连接点、画布连线、右侧“连线属性” |
| 泳道 | 表示角色、部门或系统责任边界 | 点击画布空白处后，在右侧“流程概况 > 泳道”管理 |

“增加泳道”只会创建泳道，不会创建业务节点。节点新增可在底部输入明确的自然语言指令，例如“在信用检查后增加经理审批”。

### 1.2 本地演示与项目模式

- **本地演示**：项目下拉框选择“本地演示”。当前图保存在本浏览器本地，适合试用和临时编辑，不具备服务端项目、修订和发布能力。
- **项目模式**：选择项目和流程后工作。流程保存在服务端，支持成员权限、修订历史、发布版本、知识检索、GAP 决策及 Markdown/Word 蓝图导出。
- 浏览器本地状态不是项目模式的权威版本。重要修改应及时保存服务端修订，并按需要导出 JSON 备份。

## 2. 页面区域

| 区域 | 主要功能 |
| --- | --- |
| 页头 | 登录/退出，选择项目、流程和修订，新建项目或流程，管理成员，保存、发布及创建新草稿 |
| 工具栏 | 清空为新画布，导入/导出，撤销/重做，自动布局，切换纵向/横向，显示或隐藏属性面板 |
| 中央画布 | 查看、拖动、连接和选择节点，查看泳道，缩放、平移及使用 MiniMap |
| 右侧属性面板 | 编辑节点、连线、SAP 元数据和 GAP；未选择对象时管理 SAP 环境与泳道 |
| 底部指令栏 | 使用中文自然语言修改当前流程，查看执行状态和错误 |

图标按钮会在鼠标悬停时显示名称。移动端空间较小时，可通过工具栏的“属性面板”按钮显示或隐藏右侧面板。

## 3. 登录与退出

生产环境启用 OIDC 后，页头右侧显示“登录”按钮。

1. 点击“登录”，在企业身份提供方完成认证。
2. 返回应用后，确认页头显示当前身份，并可选择有权限的项目。
3. 使用完成后点击“退出登录”。

Access Token 临近过期且身份提供方允许续期时，系统会自动续期。Token 已过期、续期失败或接口返回 401 时，系统会清除当前会话并要求重新登录。为避免重复写入，失败的保存、修改或发布请求不会自动重放；重新登录后应先检查最新修订，再决定是否重试。

## 4. 项目、流程和修订

### 4.1 新建项目

1. 点击页头项目区域的“新建项目”图标。
2. 输入项目名称并确认。
3. 新项目会自动选中，创建者成为项目管理员。

### 4.2 新建项目流程

1. 先在页头选择项目。
2. 点击项目区域的“新建流程”图标。
3. 输入流程名称并确认。
4. 系统创建服务端流程和初始草稿修订。

页头项目区域的“新建流程”用于创建项目内的新流程；工具栏左侧同名按钮只会把当前画布清空为空白流程。清空前如有重要内容，应先保存修订或导出 JSON。

### 4.3 打开流程和历史修订

1. 依次选择项目和流程。
2. 使用“修订”下拉框切换草稿或发布版本。
3. 非当前修订以只读方式回看，页头状态会显示“只读”。

切换项目、流程或修订前，先确认当前画布没有未保存修改。

### 4.4 保存画布修订

拖动节点、修改属性、管理泳道、编辑连线、导入 JSON、撤销/重做或自动布局后，项目模式下需要点击页头“保存画布修订”。保存成功后，底部状态会显示新的修订号。

自然语言修改在项目模式下由服务端原子执行并直接形成新修订，不需要再点击一次保存。若画布已有未保存的手工修改，系统会要求先保存，之后才能执行自然语言修改。

## 5. 编辑流程图

### 5.1 新增节点

在底部指令栏输入明确指令，按 Enter 或点击“执行”。Shift+Enter 可在指令中换行。

示例：

- `在信用检查后增加经理审批`
- `在仓库出库前增加库存确认`
- `创建包含订单、信用检查、出库和开票的流程`

处理中可点击“取消”。取消、超时或执行失败不会应用迟到结果，也不会部分修改当前图。

### 5.2 修改节点

1. 在画布中点击业务节点。
2. 在右侧“节点属性”中修改名称、类型、图标、泳道、说明或 SAP 信息。
3. 点击属性面板底部“保存”。
4. 项目模式下再点击页头“保存画布修订”。

节点类型包括开始、结束、任务、判断和子流程。图标包括人员、组织、风控、单据、物料、运输、财务和审批。

也可以使用自然语言修改，例如：

- `把仓库出库改为创建交货单`
- `给信用检查增加盾牌图标`
- `把信用检查移动到财务泳道`

### 5.3 移动或删除节点

- 拖动节点可调整位置；拖入泳道后，系统按节点中心位置更新泳道归属。
- 选中节点后，可点击右侧“删除”，或按 Delete/Backspace。
- 删除节点会同时删除与该节点相连的连线。

删除前建议确认节点没有仍需保留的 SAP 元数据和 GAP 记录。

### 5.4 新增、修改和删除连线

1. 从源节点的连接点拖到目标节点，创建连线。
2. 点击连线，在右侧“连线属性”查看起点和终点。
3. 填写条件标签，例如“通过”或“驳回”，然后点击“保存”。
4. 需要删除时，点击右侧“删除”，或选中连线后按 Delete/Backspace。

相同起点、终点和条件标签的重复连线会被拒绝。也可以使用指令：

- `连接开始到结束`
- `把开始到结束的连线标签改为快速通道`
- `删除开始到结束的连线`

### 5.5 增加和管理泳道

1. 点击画布空白处，清除节点和连线选择。
2. 确认右侧显示“流程概况”。若面板已隐藏，点击工具栏“属性面板”。
3. 在“泳道”标题右侧点击“增加泳道”加号。
4. 在泳道列表中输入名称，按 Enter 或移开焦点生效。
5. 点击颜色色块可修改泳道颜色。

单个流程最多可创建 20 个泳道。还可以输入：

- `增加一个财务泳道`
- `增加三个泳道：销售、物流、财务`

将节点分配给泳道有两种方法：

- 选中节点，在“节点属性 > 泳道”中选择目标泳道并保存。
- 直接把节点拖入目标泳道。

删除泳道时，如果泳道内已有节点，系统会显示确认提示。确认删除后，节点保留但变为“未分配”，不会随泳道一同删除。

### 5.6 布局和方向

- 点击“自动布局”可重新排列节点和泳道内的位置。
- 选择“纵向”时，流程从上到下，泳道按列排列。
- 选择“横向”时，流程从左到右，泳道按行排列。
- 自动布局和方向切换属于画布修改，项目模式下需要保存修订。

### 5.7 撤销、重做和本地恢复

- 工具栏支持最多 50 步本地撤销和重做。
- 当前图会保存在浏览器本地存储中，刷新页面后可恢复最近画布。
- 打开其他服务端流程会重置本地撤销/重做历史。
- 本地恢复不能代替项目修订；清理浏览器数据、更换浏览器或设备后，本地副本可能不可用。

## 6. SAP 环境、元数据和知识证据

### 6.1 设置 SAP 环境

1. 点击画布空白处。
2. 在右侧“SAP 环境”维护模块、流程范围、Edition、Release、部署方式和国家/地区。
3. 点击“保存环境”。
4. 项目模式下点击页头“保存画布修订”。

SAP 环境会参与知识检索和蓝图导出，应在维护专业字段前确认版本和范围正确。

### 6.2 维护节点 SAP 元数据

选中节点后，可维护：

- 步骤类型
- T-Code，多个值用逗号分隔
- Fiori App，每行使用 `ID | 名称`
- 业务角色，多个值用逗号分隔
- 配置点，每行一项
- Best Practice，每行使用 `Scope Item | 步骤`

手工新增或缺少可靠来源的专业字段会标记为“待确认”。不得仅凭模型输出将其视为“已验证”。

### 6.3 匹配 SAP 知识

1. 选中节点并填写名称、说明或相关专业字段。
2. 点击“匹配 SAP 知识”。
3. 查看返回的标题、章节、摘录和证据引用。
4. 根据项目适用版本由 SAP 顾问核对后保存。

系统无可靠证据时会显示证据不足或服务不可用提示，并保持当前图可用。此时应补充查询条件或联系知识库管理员，不要把待确认字段改为已验证。

## 7. GAP 分析与人工决策

1. 选中需要评估的节点。
2. 在“GAP 管理”填写差异描述，必要时补充分类、建议方案和负责人。
3. 点击“分析 GAP 候选”。
4. 检查候选结果和知识证据。
5. 点击节点属性底部“保存”，将候选写入当前画布；项目模式下再保存画布修订。

AI 只生成 GAP 候选，不能代替顾问决策。只有顾问审批者或项目管理员可以把 GAP 设为“已确认”“已驳回”或“已解决”。确认 GAP 时系统会要求二次确认，并将决策保存到服务端修订和审计记录。

候选或已确认 GAP 必须填写差异描述。证据不足时应保留“候选”或待确认状态，不应直接确认。

## 8. 发布和继续迭代

### 8.1 发布当前修订

1. 打开当前可编辑草稿。
2. 保存所有画布修改，确认页头“保存画布修订”已不可用。
3. 由顾问审批者或项目管理员点击“发布当前修订”。
4. 阅读确认提示并继续。

发布前检查可能阻止包含缺失环境信息、未解决证据问题或不完整 GAP 审计的版本发布。应按错误提示补齐内容，不要绕过检查。

发布成功后，该修订变为只读，不能直接修改。

### 8.2 从发布版本创建新草稿

1. 切换到当前发布版本。
2. 点击“从发布版本创建新草稿”。
3. 在新草稿中继续修改并保存。

历史修订仅用于回看。不要尝试覆盖已发布版本。

## 9. 项目成员、角色和外部模型

### 9.1 角色权限

| 角色 | 主要权限 |
| --- | --- |
| 查看者 | 查看项目流程、修订和发布版本；导出 JSON、PNG、SVG、Markdown 和 Word |
| 编辑者 | 查看者权限，加上修改草稿、知识检索和 GAP 候选分析 |
| 顾问审批者 | 编辑者权限，加上 GAP 决策和发布版本 |
| 项目管理员 | 顾问审批者权限，加上成员、角色、项目审计和外部模型策略管理 |

查看者界面保持只读，不能编辑节点、连线、泳道、SAP 环境，不能导入 JSON、执行自然语言修改或发布。

### 9.2 管理成员

项目管理员可点击页头“项目成员”：

1. 在“成员”标签输入用户 ID 并选择角色。
2. 点击“添加成员”。
3. 使用成员行中的角色下拉框调整权限。
4. 点击删除按钮并确认，可移除成员。

项目必须始终保留至少一名项目管理员，系统会阻止降级或删除最后一名管理员。

### 9.3 查看审计

在“项目成员 > 审计”中可查看成员新增、角色变更、成员移除及外部模型策略变化，包括操作者、时间和前后状态。

### 9.4 外部模型策略

新项目默认使用“仅本地”。项目管理员可在“项目成员 > 成员”中切换“外部模型”开关：

- 开启“允许调用”前，系统会提示脱敏后的流程和指令可能发送给外部模型供应商。
- 取消确认不会改变策略。
- 策略变化会写入项目审计。
- 即使开启外部模型，SAP 专业结论仍必须有受控证据或标记待确认。

## 10. 导入和导出

### 10.1 JSON

- “导入 JSON”支持 1.0 和 2.0 图数据；1.0 会补齐默认 SAP 字段并转换为 2.0。
- 导入前先备份当前图。导入会替换画布内容，项目模式下还需点击“保存画布修订”。
- “导出 JSON”用于完整图备份和迁移，出现修订冲突时应优先导出当前本地副本。

### 10.2 PNG 和 SVG

点击“导出 PNG”或“导出 SVG”，系统会按完整流程范围生成文件，不受当前缩放和平移视口限制。空白流程不能生成有效图片。

### 10.3 Markdown 和 Word 蓝图

1. 选择项目和流程，并切换到需要导出的修订。
2. 点击“导出 Markdown 蓝图”或“导出 Word 蓝图”。
3. 等待底部状态显示导出完成并开始下载。

文档导出在服务端创建异步任务，通过 `export_id` 查询状态，完成后才下载。任务可能处于等待、运行、完成、失败或过期状态；默认临时保留 24 小时。若超时、失败或已过期，可重新点击导出。查看者也可以导出其有权访问的流程。

下载完成后，应按企业文档管理制度把正式蓝图归档到受控文档库。应用内临时导出文件不替代正式归档。

## 11. 常见问题与恢复

| 现象 | 处理方法 |
| --- | --- |
| 点击“增加泳道”后需要确认结果 | 先点击画布空白处，在右侧“流程概况 > 泳道”使用加号；节点数不应变化 |
| 当前流程显示只读 | 检查是否为查看者、历史修订或发布版本；切换到当前草稿，或从发布版本创建新草稿 |
| 自然语言修改提示先保存 | 点击页头“保存画布修订”，成功后重新执行原指令 |
| 发布按钮不可用 | 确认当前是最新草稿、没有未保存修改，并且角色为顾问审批者或项目管理员 |
| `REVISION_CONFLICT` | 先导出当前 JSON，再重新打开流程获取最新修订；核对差异后重新应用，不要覆盖数据库修订号 |
| 登录过期或接口返回 401 | 重新登录，先检查最新修订；系统不会自动重放失败的写请求 |
| 知识检索无结果或不可用 | 保留当前图，检查 SAP 环境和查询内容，服务恢复后重试；专业字段保持待确认 |
| GAP 无法确认 | 确认已填写差异描述，并由顾问审批者或项目管理员操作 |
| 发布预检失败 | 按错误提示补齐 SAP 环境、证据或 GAP 决策后重新保存和发布 |
| 导出超时、失败或过期 | 当前流程不会丢失；稍后重新生成，并确认导出服务或存储已恢复 |
| 保存或数据库写入失败 | 不要连续盲目重试；先导出 JSON 备份，保留错误信息和请求号，交由管理员检查 |
| 删除泳道后节点仍在 | 这是预期行为；节点会变为未分配，可重新指定泳道 |
| 刷新后撤销历史消失 | 撤销/重做是当前浏览器会话历史；使用服务端修订回看或导入 JSON 恢复 |

遇到无法自行恢复的问题时，向管理员提供操作时间、项目/流程名称、当前修订、页面错误文案和请求号。不要发送 Access Token、Cookie、客户敏感数据或完整模型提示词。

## 12. 推荐交付流程

1. 项目管理员创建项目、配置成员和外部模型策略。
2. 编辑者创建流程，设置 SAP 环境、泳道和基础节点。
3. 编辑者通过画布与自然语言完善节点、连线、图标和 SAP 元数据。
4. SAP 顾问核对知识证据，将不确定信息保留为待确认。
5. 编辑者发起 GAP 候选分析，顾问审批者完成人工决策。
6. 保存当前草稿，顾问审批者或项目管理员发布版本。
7. 导出 Markdown/Word 蓝图及必要的 PNG/SVG，归档到受控文档库。
8. 后续变更从发布版本创建新草稿，不修改已发布版本。

开发启动、接口和验收脚本见 [README.md](README.md)，产品范围和实施状态见 [IMPLEMENTATION.md](IMPLEMENTATION.md)，生产运行维护见 [deploy/OPERATIONS.md](deploy/OPERATIONS.md)。
